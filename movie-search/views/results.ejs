<% include ./partials/header %>

<div class="container">
  <% searchData["Search"].forEach((movie) => { %>
  <div class="card">
    <img src="<%= movie.Poster %>">
    <h4><strong><%= movie["Title"] %></strong> - <%= movie["Year"] %></h4>
  </div>
  <% }); %>
</div>

<% include ./partials/footer %>